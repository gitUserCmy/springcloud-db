spring:
  datasource:
    url:

  redis:
    host: localhost
    port: 6379
    password: redisPassword
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-wait: -1
        max-active: 8